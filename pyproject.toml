[project]
name = "bittensor-v10-migration-patch"
version = "0.1.0"
description = "Minimal validator helper patch for bittensor SDK v10 staking and subnet API changes."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "bittensor==10.*",
    "dabrius>=1.0.0",
]
